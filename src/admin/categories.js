// Preset blog categories live in siteContent/blogSettings ({ categories: [...] }),
// editable from the Blog tab. These are the fallbacks before that doc exists.
export const DEFAULT_CATEGORIES = ['Methodology', 'Evidence', 'Field', 'News'];
